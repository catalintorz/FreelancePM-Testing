@page
@model FreelancePM.Pages.ProjectTasks.EditModel

@{
    ViewData["Title"] = "Edit Task";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-6 col-md-8">

            <h1 class="mb-1">Edit Task</h1>
            <p class="text-muted mb-4">Update task details.</p>

            <div class="card shadow-sm">
                <div class="card-body">
                    <form method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                        <input type="hidden" asp-for="WorkTask.Id" />

                        <div class="mb-3">
                            <label asp-for="WorkTask.Title" class="form-label"></label>
                            <span class="text-danger">*</span>
                            <input asp-for="WorkTask.Title" class="form-control" />
                            <span asp-validation-for="WorkTask.Title" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="WorkTask.Description" class="form-label"></label>
                            <input asp-for="WorkTask.Description" class="form-control" />
                        </div>

                        <div class="mb-3">
                            <label asp-for="WorkTask.Deadline" class="form-label"></label>
                            <span class="text-danger">*</span>
                            <input asp-for="WorkTask.Deadline" type="date" class="form-control" />
                            <span asp-validation-for="WorkTask.Deadline" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="WorkTask.ProjectId" class="form-label"></label>
                            <span class="text-danger">*</span>
                            <select asp-for="WorkTask.ProjectId"
                                    class="form-select"
                                    asp-items="ViewBag.ProjectId">
                            </select>
                        </div>

                        <div class="mb-4">
                            <label asp-for="WorkTask.StatusId" class="form-label"></label>
                            <span class="text-danger">*</span>
                            <select asp-for="WorkTask.StatusId"
                                    class="form-select"
                                    asp-items="ViewBag.StatusId">
                            </select>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a asp-page="./Index" class="btn btn-outline-secondary">
                                Back to list
                            </a>
                            <button type="submit" class="btn btn-primary">
                                Save
                            </button>
                        </div>
                    </form>
                </div>
            </div>

        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
